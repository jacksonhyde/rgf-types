export declare namespace Page {
    interface Config {
        buttons: [
            {
                style: string;
                href: string;
                start?: boolean;
            }
        ];
    }
}
